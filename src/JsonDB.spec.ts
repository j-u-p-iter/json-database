import { JsonDB } from '.';

describe('JsonDB', () => {
  let db;

  beforeEach(() => {
    db = new JsonDB(); 
  });

  describe('scan', () => {
    it('returns no data for the empty database', () => {
    });

    it('returns all data for the filled in database', () => {

    });
  });
});
